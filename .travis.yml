os: linux
dist: jammy

jobs:
  include:
    - name: "job one"
      vm:
        create:
          name:  my_custom_name #custom identifier string
      script:
        - echo "condition with job oneeee"
        - echo $TRAVIS_COMMIT_MESSAGE
        - sleep 50
